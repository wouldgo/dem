/*global describe,it,expect*/

describe('app', () => {
  'use strict';

  it('passes a test', () => {
    expect(true).toBe(true);
  });

  /*it('fails a test', () => {
    expect(1).toBe(0, 'intentionally failing test');
  });*/
});
